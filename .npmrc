# Monorepo peer dependency resolution
# Packages in this workspace have cross-references; legacy resolution avoids
# spurious ERESOLVE errors when mixing stable and beta versions.
legacy-peer-deps=true
